- name: vars
  hosts: node
  vars:
   course: "terraform"
   trainer: "shiva"
   duration: "12 hrs"
  tasks:
  - name: display information
    vars:
     course: "ansibe"  
    ansible.builtin.debug:
     msg: "hi my trainer {{trainer}} course {{course}} duration {{duration}}"
  
  tasks:
  - name: display information 
    ansible.builtin.debug:
    msg: "hi my trainer {{trainer}} course {{course}} duration {{duration}}"

    

